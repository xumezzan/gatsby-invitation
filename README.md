# Latifa's Great Birthday — Gatsby Invitation

Одностраничный веб-пригласительный в стиле «Великий Гэтсби» (арт-деко, золото/серебро/чёрный).
Дата события: **12.07.2026**, место: **Miras, Ташкент**.

Чистый HTML/CSS/JS в одном файле. Без сборки, без зависимостей, без фреймворков.

## Быстрый старт

```bash
# вариант 1: просто открыть index.html в браузере

# вариант 2: локальный сервер (нужен для теста музыки на некоторых браузерах)
npx serve .
# или
python3 -m http.server 8000
```

Проверять обязательно в мобильной ширине (~380px) — приглашение рассылается через Telegram.

## Деплой

Netlify Drop: перетащить **папку целиком** (index.html + music.mp3) на https://app.netlify.com/drop.
Пока музыки нет, можно кидать один index.html — сайт полностью рабочий и без неё.

## Структура

```
latifa-invitation/
├── index.html                      # весь сайт: разметка + стили + скрипты + base64-картинки
├── assets/
│   ├── ladies.png                  # иллюстрация дресс-кода (уже вшита в index.html как base64)
│   ├── gentlemen.png               # то же
│   └── source/
│       ├── figures-original.png    # исходник иллюстраций (обе фигуры, прозрачный фон)
│       └── reference-invitation.jpg# дизайн-референс (печатное приглашение)
├── CLAUDE.md                       # память проекта для Claude Code (читать первым!)
├── .cursorrules                    # правила для Cursor (ссылается на CLAUDE.md)
└── README.md
```

`assets/` в деплой не нужен — это исходники на случай доработок. На сайте картинки уже внутри index.html.

## Как устроена страница

Интро (чёрный экран → искры → прорисовка рамки → текст → кнопка ENTER THE PARTY → золотая полоса → «двери» открываются) → Hero-карточка → Countdown → Details (When/Where) → Dress code (палитра + иллюстрации) → Финальная цитата → футер. Музыка стартует по клику на ENTER THE PARTY, кнопка вкл/выкл — справа внизу.

## Что кастомизировать

| Что | Где в index.html |
|---|---|
| Время начала (countdown) | константа `EVENT_DATE` в начале `<script>` |
| Время в карточке When | комментарий `<!-- Когда будет известно время начала -->` |
| Адрес зала + ссылка на карту | комментарий в карточке Where |
| Фото дресс-кода вместо иллюстраций | блоки `<!-- ФОТО-ПРИМЕР 1/2 -->`, поменять `src` у `<img>` |
| Музыка | положить `music.mp3` рядом с index.html (см. блок `<!-- ===== МУЗЫКА -->`) |
| Текст кнопки входа | `#enterBtn` (`Enter the party` → `Open invitation`) |
| Цвета и шрифты | CSS-переменные в `:root` |

## TODO

- [ ] Уточнить время начала → обновить `EVENT_DATE` и карточку When
- [ ] Точный адрес Miras + ссылка на Яндекс.Карты/2GIS в карточку Where
- [ ] `music.mp3` — тихий джаз 1920-х, royalty-free (pixabay.com/music: «1920s jazz», «swing»)
- [ ] Прогнать на реальном телефоне с увеличенным системным шрифтом (баг уже чинили — регресс-чек)

Бэклог идей: RSVP через Telegram-бота или Google Form, кнопка «Add to calendar» (.ics), OG-картинка 1200×630 для превью в мессенджерах.
