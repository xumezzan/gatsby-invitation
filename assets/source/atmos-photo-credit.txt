Atmosphere (screen 3): Pexels ID 14222876 (champagne pour) — https://www.pexels.com/photo/14222876/
  Royalty-free. Processed to gold duotone (Pillow).

Dress code (screen 4): OWNER'S OWN photos (dresscode-her.jpg / dresscode-him.jpg).
  Already warm-gold on black — no duotone applied, only resized to 860px + optimized.
  Copies: assets/dresscode-her-duotone.jpg, assets/dresscode-him-duotone.jpg

Atmosphere (screen 3) UPDATED: now a background VIDEO (owner's "Generated video 1.mp4",
champagne pour). Deployed as atmos.mp4 next to index.html. Encoded via ffmpeg:
boomerang (forward+reverse) for seamless loop, no audio, faststart, CRF 28.
Poster = full-glass frame, inline base64.

ZOOM-OUT PASS (owner: «слишком близко»): re-encoded with scale + pad to 720x1280,
pad color = 0x0A0A0C (EXACTLY the site background --bg #0a0a0c) so черные поля вокруг
кадра сливаются с фоном — при object-fit:cover в CSS шва/«квадрата» не видно.
CSS в .atmos-bg вернули на object-fit:cover (было contain — давало видимый прямоугольник,
т.к. края видео чуть светлее фона). object-fit:contain НЕ использовать — вернёт квадрат.

UPSCALED SOURCE (owner прислал upscaled-video-2.mp4, 1440x2560, 24fps, 10s, со звуком):
исходник уже скадрирован ШИРЕ прежнего — бокал не во весь кадр, воздух вокруг. Поэтому
доп. zoom-out НЕ нужен (пробовал 0.9 — бокал мельчал зря). Пайплайн: downscale→720x1280,
бумеранг (fwd+reverse concat) для бесшовного лупа, -an (звук убрать), CRF 28, +faststart.
Итог ~1.65MB. Резкость заметно лучше прежнего (пузырьки/грани). scale=1.0 (без pad).
assets/atmos-source-boomerang.mp4 — текущая апскейл-версия (источник дальнейших правок).
Бэкапы: atmos-source-boomerang-CLOSE-backup.mp4 (самый первый крупный план),
atmos-prev-78pct-backup.mp4 (промежуточный zoom-out 0.78 старого исходника).
Отдалить/приблизить эту версию — менять scale в ffmpeg (+pad #0a0a0c если <1), НЕ CSS.

Palette swatches (dress code): owner-supplied metallic circles (black silk+gold ring / brushed gold / brushed silver).
Extracted from a checkerboard-bg PNG via circular alpha mask (Pillow), quantized to ~8KB each, inline base64.
Copies: assets/swatch-*.png
